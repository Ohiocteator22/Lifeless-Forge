sv-ttk>=2.6.0
tkinterdnd2>=0.3.0
pyzipper>=0.3.6
tqdm>=4.66.0
zstandard>=0.22.0
---------------------------PIP INSTALL--------------------------------
```
  pip install sv-ttk
  pip install tkinterdnd2
  pip install pyzipper
  pip install tqdm
  pip install zstandard
```
