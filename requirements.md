# Core (no mandatory third‑party dependencies)
# Optional extras:
#   encryption: pyzipper
#   zstd: zstandard
#   gui: sv-ttk tkinterdnd2
#   progress: tqdm
# Install all with: pip install -e .[all]
